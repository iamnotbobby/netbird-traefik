[entryPoints]
  [entryPoints.http]
    address = ":80"
  [entryPoints.https]
    address = ":443"

[providers.docker]
  endpoint = "unix:///var/run/docker.sock"

[providers.file]
  filename = "/etc/traefik/services.toml"

[certificatesResolvers.webssl.acme]
  email = "admin@domain.com"
  storage = "acme.json"
  caServer = "https://acme-v02.api.letsencrypt.org/directory"
  [certificatesResolvers.webssl.acme.dnsChallenge]
    provider = "cloudflare"
    delayBeforeCheck = 0
    
[http.middlewares]
  [http.middlewares.redirection.redirectScheme]
    scheme = "https"
    permanent = true
    sslRedirect = true
    
# Autorize TSL backend
# [serversTransport]
#   insecureSkipVerify = true
